# Dev-Bot — Overview

- Workflows: `.github/workflows/dev-bot.yml`, `dev-bot-daily-plan.yml`
- Purpose: PR example generator; daily planning Issue at 06:00 ET (self-gated)
- Creates branch `cursor/dev-bot/example-<run_id>` and opens a PR via `create-pull-request`
